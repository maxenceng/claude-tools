/**
 * Training: a worked example, not a domain to build on (ADR 0008). A {@code Course} whose
 * {@code popularity} an external training catalogue vendor fills — nothing else. Delete this
 * context, or rename it into your own first one, whichever comes first.
 */
@ApplicationModule
package com.example.app.training;

import org.springframework.modulith.ApplicationModule;
