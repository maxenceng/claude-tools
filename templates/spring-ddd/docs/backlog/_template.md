---
id: CONTEXT-0
status: todo
context: <bounded context, or cross-context>
type: feature
created: YYYY-MM-DD
---

# <One sentence, in the language of the domain>

## Acceptance criteria

- [ ] <Observable behaviour, not implementation>
- [ ] <The case that fails — every rule has one, and it is the one that gets forgotten>

## Model decision

<Where the behaviour belongs and why. Filled in before coding starts, not after.
New behaviour on an existing aggregate? A new aggregate? A new context? If the answer
is "a new context", stop and say so — that is a bigger change than a ticket.>

## Glossary impact

<Terms this adds, and any word it displaces. Both go in docs/glossary.md in the same
change. If none, write "none" so a reader knows it was considered.>

## Notes

<Anything learned while doing it that the code will not show — an approach tried and
rejected, a constraint discovered. Empty is fine.>
