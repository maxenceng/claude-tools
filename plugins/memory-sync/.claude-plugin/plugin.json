{
  "name": "memory-sync",
  "description": "Mirror ~/.claude/projects/*/memory, and per-project docs, into self-hosted Qdrant collections for semantic search, with an MCP server exposing search/sync as tools; remind Claude to capture new memory after PR feedback and before context compaction, and sync automatically at session end",
  "version": "1.3.0",
  "author": {
    "name": "maxenceng"
  }
}
